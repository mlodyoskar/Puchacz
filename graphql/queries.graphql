query GetAllEvents {
	events {
		id
		name
		slug
		day
		stuffs {
			...StuffData
		}
	}
}

fragment StuffData on Stuff {
	id
	name
	type
}

query GetAllStuff {
	stuffs {
		...StuffData
	}
}

query GetEventById($id: ID!) {
	event(where: { id: $id }) {
		createdAt
		name
		id
		day
		participients
		ticketPrice
		stuffs {
			id
			name
			type
		}
		image {
			url
		}
		budgets {
			id
			name
			amount
			isIncome
		}
	}
}
